我現在想建立一個網站，目前沒有要廣大經營，但還是需要一定的登入系統，讓我跟朋友們可以連線玩遊戲。

這個網站有點類似 BoardGameArena，目標是上面可以有不少個遊戲（桌遊、益智遊戲），可以是 BoardGameArena 上面沒有的桌遊、某些桌遊的客製化、自製遊戲。

初期我們會先導入幾款遊戲的數位化，之後我們會希望可以開始把一些核心邏輯抽出來，之後抽取出來的核心邏輯架構可以作為遊戲編輯器，以及模組化其他遊戲的一大利器。