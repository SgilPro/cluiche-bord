# Database Configuration
# 從 Supabase 專案設定中取得連線字串
# 格式：postgresql://[user]:[password]@[host]:[port]/[database]?[parameters]

# 主要資料庫連線（用於 Prisma Client）
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# 直接連線（用於 Migration，Supabase 通常需要）
# 如果 Supabase 使用 connection pooling，需要設定 DIRECT_URL
DIRECT_URL="postgresql://user:password@host:port/database?sslmode=require"

# 注意事項：
# 1. 請將此檔案複製為 .env.local 並填入實際的連線字串
# 2. .env.local 不會被提交到 Git（已在 .gitignore 中）
# 3. 連線字串包含敏感資訊，請妥善保管
# 4. Supabase 連線字串可以在專案設定 > Database > Connection string 中找到

# Network Configuration (for WLAN/local network access)
# 如果要讓其他設備透過 WLAN 掃描 QR Code 加入房間，需要設定以下環境變數

# 基礎網址（用於 QR Code 和連結）
# 設定為你的區域網路 IP，例如：http://192.168.0.117:3000
# 如果不設定，會使用 localhost（只能本機訪問）
NEXT_PUBLIC_BASE_URL="http://YOUR_LOCAL_IP:3000"

# Socket.IO 伺服器網址（用於即時通訊）
# 設定為你的區域網路 IP，例如：ws://192.168.0.117:4001
# 如果不設定，會使用 ws://localhost:4001（只能本機連線）
NEXT_PUBLIC_SOCKET_URL="ws://YOUR_LOCAL_IP:4001"

# 如何找出你的區域網路 IP：
# macOS/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
# Windows: ipconfig
# 或執行: ./scripts/find-local-ip.sh
